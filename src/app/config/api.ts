export const DEVELOPER = "n.lipnik";
export const API_URL = `https://uxcandy.com/~shapoval/test-task-backend/v2`;
export const MAX_TASKS_PER_PAGE = 3;

export const ERRORS_MESSAGES: Record<string, Record<string, string>> = {
  ru: {
    required: "Поле не должно быть пустым",
    email: "Не корректный email адрес",
  },
};
